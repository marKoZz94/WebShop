import styled from 'styled-components';
import colors from '../../../config/colors';

const H3 = styled.h3`
  font-size: 18px;
  font-weight: 900;
  color: ${colors.BLACK_COLOR};
  margin: 0 0 16px;
`;

export default H3;
