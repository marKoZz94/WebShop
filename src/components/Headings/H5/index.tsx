import styled from 'styled-components';
import colors from '../../../config/colors';

const H5 = styled.h5`
  font-size: 22px;
  font-weight: 900;
  color: ${colors.BLACK_COLOR};
  margin: 0 0 16px;
`;

export default H5;
