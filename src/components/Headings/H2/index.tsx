import styled from 'styled-components';
import colors from '../../../config/colors';

const H2 = styled.h2`
  font-size: 22px;
  font-weight: 900;
  color: ${colors.BLACK_COLOR};
  margin: 0 0 16px;
`;

export default H2;
