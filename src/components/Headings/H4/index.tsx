import styled from 'styled-components';
import colors from '../../../config/colors';

const H4 = styled.h4`
  font-size: 24px;
  font-weight: 900;
  color: ${colors.BLACK_COLOR};
  margin: 0 0 16px;
`;

export default H4;
